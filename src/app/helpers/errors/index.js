const CustomAPIError = require('./custom-err')
const UnauthenticatedError = require('./unaAuthenticated')
const NotFoundError = require('./notfound')
const BadRequestError = require('./bad-request')

module.exports = {
  CustomAPIError,
  UnauthenticatedError,
  NotFoundError,
  BadRequestError,
}
